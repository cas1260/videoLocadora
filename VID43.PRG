**************************************
*	 MANUTE€ÇO DO PROGRAMA VID42.PRG	 *
**************************************
SAVE SCREEN TO VTELA_MAT
@ 05,01 CLEAR TO 23,77
@ 06,18 TO 20,60 DOUBLE
CENTRA(07,19,59,"N§ da Copia 00001")
CENTRA(08,19,59,"Todos os Direitos Reservados A:")
CENTRA(09,19,59,"CLEBER DE ALMEIDA SOARES")
@ 10,19 TO 10,60 DOUBLE
@ 10,18 SAY "Ì"
@ 10,60 SAY "¹"
	USE LOCACAO
	VLOCACAO_TOTAL =  INT((RECSIZE() * LASTREC()) + HEADER() + 1)
	VLOCACAO_QTD = LASTREC()
	USE LOCACAO2
	VLOCACAO2_TOTAL =  INT((RECSIZE() * LASTREC()) + HEADER() + 1)
	VLOCACAO2_QTD = LASTREC()
	USE CLIENTE
	VCLIENTE_TOTAL =  INT((RECSIZE() * LASTREC()) + HEADER() + 1)
	VCLIENTE_QTD = LASTREC()
	USE PRODUTO
	VPRODUTO_TOTAL =  INT((RECSIZE() * LASTREC()) + HEADER() + 1)
	VPRODUTO_QTD = LASTREC()
	CENTRA(11,18,60,"TOTAL DE LOCA€ÇO    " + STRZERO(VLOCACAO_QTD,10))
	CENTRA(12,18,60,"TOTAL DE FITAS LOCA " + STRZERO(VLOCACAO2_QTD,10))
	CENTRA(13,18,60,"TOTAL DE CLIENTES   " + STRZERO(VCLIENTE_QTD,10))
	CENTRA(14,18,60,"TOTAL DE PRODUTOS   " + STRZERO(VPRODUTO_QTD,10))
	@ 15,18 TO 15,60 DOUBLE
	@ 15,18 SAY "Ì"
	@ 15,60 SAY "¹"
	@ 16,19 SAY "Espa‡o Ocupante  "
	VTOTAL_TOTAL = VPRODUTO_TOTAL+VCLIENTE_TOTAL+VLOCACAO2_TOTAL+VLOCACAO_TOTAL
	VTOTAL_TOTAL = STR(VTOTAL_TOTAL)
	@ 16,40 SAY VTOTAL_TOTAL
	@ 16,52 SAY " kb"
	@ 17,18 TO 17,60 DOUBLE
	@ 17,18 SAY "Ì"
	@ 17,60 SAY "¹"
	CENTRA(18,18,60,"Autorizado Para")
	CENTRA(19,18,60,VEMPRESA)
	SET CURSO OFF
	DO WHILE .T.
		A=INKEY()
		IF A=13 .OR. A=27
			EXIT
		ENDIF
	ENDDO
	SET CURSO ON
	CLOSE ALL
RESTORE SCREEN FROM VTELA_MAT
RETURN